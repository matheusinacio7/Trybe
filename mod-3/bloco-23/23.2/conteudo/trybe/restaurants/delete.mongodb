use('trybe');

// Remova o primeiro restaurante que possua culinária do tipo Ice Cream, Gelato, Yogurt, Ices .

db.getSiblingDB('trybe').restaurants.findOne({ cuisine: 'Ice Cream, Gelato, Yogurt, Ices' } );

db.getSiblingDB('trybe').restaurants.deleteOne({ cuisine: 'Ice Cream, Gelato, Yogurt, Ices' });


// Remova todos os restaurantes que possuem culinária do tipo American .

db.getSiblingDB('trybe').restaurants.deleteMany({ cuisine: 'American' });
